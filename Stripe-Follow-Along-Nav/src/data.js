
/**
 * background and nav are global variables that will be import in handlers.
 * triggers is a glob variable that will be import in listeners
 */

export const triggers = document.querySelectorAll('.cool > li');
export const background  = document.querySelector('.dropdownBackground');
export const nav  = document.querySelector('.top');